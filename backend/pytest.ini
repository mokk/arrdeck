[pytest]
pythonpath = .
asyncio_mode = auto
# pytest-asyncio 1.x warns until this is stated; "function" is the value it
# will default to, so setting it now is a no-op that silences the notice
asyncio_default_fixture_loop_scope = function
